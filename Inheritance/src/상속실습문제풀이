2-1
매개변수생성자A30
생성자B

2-2
super();는 생성자 첫 줄에 와야함.

3
생성자A:10
생성자A
생성자B:11

5
class A의 기본생성자가 존재하지 않음.
or super(int);가 존재하지 않음.

6
super(size);

9
Parent(String nation) call
Parent() call
Child(String name) call
Child() call

4(2)
public int getSum(){
    return n + i + j; // j 오류
}
//이유
private 접근제어자로 선언된 맴버는 해당 클래스 내부에서만 접근 가능.
//수정
public int getSum(){
    return n + i + getJ();
}
or
private를 protected로 변경

7
test1_8.Main.java

8
test1_9.Work.java

4(1)
스노우 타이어가 굴러갑니다.
스노우 타이어가 굴러갑니다.

10
practice_10

11
practice_11

메서드 오버라이딩(상속x)
메서드 선언 시 메서드명은 중복 불가
하지만 매개변수의 갯수, 매개변수의 자료형을 다르게 선언하면 메서드명을 중복으로 사용할 수 있다.

메서드 오버라이딩(상속o)
상속 관계에서 부모클래스의 메서드를 자식클래스에서 동일하게 재정의 하는 것을 말한다.
자식 클래스에서 메서드를 재정의하면 자식 클래스의 객체에서는 더이상 부모클래스에서 상속받은 메서드를 호출할 수 없다.